# Frontend-only dependencies (Streamlit Community Cloud).
# Heavy ML packages (xgboost, lightgbm, shap, sklearn) live in
# backend/requirements.txt — the frontend only reads pre-computed artifacts.
streamlit>=1.36
pandas>=2.0
altair>=5.0
requests>=2.31
# FullCalendar wrapper — per-sport valid-date highlighting in the Today's
# Games date picker (st.date_input cannot highlight/disable arbitrary dates).
streamlit-calendar>=1.4,<2
