# Canonical decided frame is byte-hashed for provenance (frame sha 3e8c8a510f04);
# pin LF so any checkout reproduces the same sha (CRLF churn broke this).
nfl-backend/data_delivery/nfl_game_level_features.csv text eol=lf
