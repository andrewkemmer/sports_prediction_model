__pycache__/
*.pyc
*.pyo
.env
*.joblib

# Generated per-run artifacts — the pipeline re-creates them on every run,
# so they are never committed (serving families ship fresh each run).
data_delivery/*.csv
data_delivery/*.json
data_delivery/*.meta.json
