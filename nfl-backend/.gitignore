__pycache__/
*.pyc
*.pyo
.env
*.joblib

# Generated artifacts — produced by backend/nfl_game_frame.py; the module
# re-creates them on every run, so they are never committed (see README).
data_delivery/*.csv
