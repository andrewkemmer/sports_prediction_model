# Python byte-cache artifacts from imports at repo root
__pycache__/
*.pyc
.pytest_cache/

# Regenerable run-line bridge artifact (read-only stand-in for the next
# pipeline run, produced by run_rl_bridge.py) — never committed.
mlb-backend/data_delivery/run_engine_markets_*_rl.csv

# Python byte-cache from local imports/test runs — never committed.
__pycache__/
*.pyc
.pytest_cache/

# Agent/user scratch lists (purge manifests etc.) — never committed.
mlb-backend/backend/final_delete.txt

# Coding-agent scratch workspace: ablations, verification harnesses, tests.
# Never committed — verdicts land as mlb_* records in data_delivery/ (README §4/§5).
scratch/