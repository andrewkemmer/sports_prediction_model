# Streamlit base theme — matches the dark palette injected by utils.inject_css()
[theme]
base = "dark"
backgroundColor = "#0A1128"
secondaryBackgroundColor = "#1E293B"
primaryColor = "#10B981"
textColor = "#E2E8F0"

[server]
headless = true
