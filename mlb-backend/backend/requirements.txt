GitPython>=3.1.0
pybaseball>=2.2.0
# Version-guarded: shap's XGBoost loader requires the base_score decode shim
# in backend/explainability.py for xgboost>=2 UBJSON dumps (verified pair:
# xgboost 3.2 + shap 0.49). Pin was originally validated against the SHAP
# shim unit tests, which now live only in git history.
xgboost>=1.7,<4
lightgbm
scikit-learn
shap>=0.45,<0.51
pandas
numpy
joblib
requests
duckdb>=1.0.0
pyarrow
