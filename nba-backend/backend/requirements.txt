pandas
numpy
scipy
scikit-learn
xgboost
lightgbm
joblib
# The artifact publisher. github_sync.py drives the delivery push - clone,
# stage, commit, retry on rejection, verify the remote tree - with GitPython
# rather than hand-rolled ``git`` subprocesses. Pinned the same floor MLB uses;
# without it a Kaggle run reaches the delivery phase and dies on ImportError,
# after the model has already finished.
GitPython>=3.1.0
# The Parquet engine. Never imported by name: pandas reaches it through
# to_parquet/read_parquet, and the cache is Parquet end to end.
pyarrow
openpyxl
pytest
# The progress bar. MLB's notebook installs tqdm by name for the same reason:
# without it the pull reports only a heartbeat log line, and the captured
# Kaggle run is a wall of numbers with no bar to watch.  ``progress.py``
# imports it lazily, so a missing tqdm costs legibility, never the run.
tqdm
# duckdb and requests are no longer pinned. duckdb was here to read the raw
# archive export this backend never had a reader for, and requests is unused —
# ingestion speaks urllib from the standard library.
