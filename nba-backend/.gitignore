__pycache__/
*.pyc
.pytest_cache/
# The normalized cache is written outside the repository (NBA_CACHE_DIR, or
# ~/.cache/sports_prediction_model/nba). The old .nba_cache/ rule described a
# location nothing has written to since the cache moved.
